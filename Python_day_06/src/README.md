Для запуска скриптов необходимо установить виртуальное окружение!

ex03:
- Устанавовить psql
- Запустить скрипт `python3 create_db.py`, где необходимо заменить переменные `CURRENT_DB_NAME`, `USER`, `NEW_DB_NAME` на свои.
